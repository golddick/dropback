# Database (Prisma — Supabase's Postgres connection string works fine here)
DATABASE_URL=

# Supabase — Realtime only now (DB access moved to Prisma above)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# DropAPHI — OTP + screenshot uploads (see dropaphi.xyz/docs)
DROPAPHI_API_KEY=
DROPAPHI_BASE_URL=https://dropaphi.xyz/api

# UploadThing — video uploads
UPLOADTHING_TOKEN=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Email notifications (Resend)
RESEND_API_KEY=
NOTIFY_FROM_EMAIL=notify@dropback.app
